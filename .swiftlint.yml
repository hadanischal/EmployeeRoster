opt_in_rules:
    - empty_count

excluded: # paths to ignore during linting. Takes precedence over `included`.
    - Carthage
    - Pods
    - EmployeeRosterTests/GeneratedMocks.swift

line_length:
- 300 # warning
- 400 # error
